@model CineWeb.Models.ShowTime

@{
    ViewData["Title"] = "Seat Selection";
}

<body>
    <h2>@Model.MovieId.Title</h2>
    <form>
        <table>
            @for(byte i = Model.TheaterId.NumRows; i > 0; i--){
                @:<tr>
                    @for(byte j = 1; j <= Model.TheaterId.NumCols; j++){
                        @:<td>
                            @:<button class="btn btn-primary" disabled="false">@Convert.ToChar(i + 64)@String.Format("{0:00}", j)</button>
                        @:</td>
                    }
                @:</tr>
            }
        </table>
    </form>
</body>

