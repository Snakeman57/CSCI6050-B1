@model CineWeb.Models.Role

@{
    ViewData["Title"] = "Details";
}

<h1>@Html.DisplayFor(model => model.Roles) Details</h1>

<div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Roles)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Roles)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn bg-primary text-white" >Edit</a> 
    <a asp-action="Index" class="btn bg-primary text-white">Back to List</a>
</div>
